<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Main background and border -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/white" />
            <stroke android:width="1dp" android:color="@color/colorDivider" />
        </shape>
    </item>
    <!-- Top accent border -->
    <item android:bottom="1dp" android:left="0dp" android:right="0dp">
        <shape android:shape="rectangle">
            <stroke android:width="4dp" android:color="@color/colorPriorityHigh" />
        </shape>
    </item>
    <!-- Mask to hide other borders of the accent -->
    <item android:top="4dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/white" />
        </shape>
    </item>
</layer-list>